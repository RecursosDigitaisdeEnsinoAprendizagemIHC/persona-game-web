import styled from "styled-components";

export const Title = styled.div`
  margin-top: 30px;
  font-size: 2.25rem;
  line-height: 52px;
  color: ${(props) => props.theme.black};
  text-align: center;
`;
